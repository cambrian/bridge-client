    export function {{ spec.route }} (
      bridgeClient: BridgeClient.T<Server.{{ service.namespace }}>,{% if spec.auth %}
      token: Text<'AuthToken'>,{% endif %}
      request: {{ spec.req }}
    ): {% if spec.streaming %}Stream{% else %}Promise{% endif %}<{{ spec.res }}> {
      return {% if spec.streaming %}streaming{% else %}direct{% endif %}<{{ spec.req }}, {{ spec.res }}, Server.{{ service.namespace }}>(
        bridgeClient,
        {{ spec.timeoutMillis }},
        '{{ spec.namespacedRoute }}',
        request,
        '{{ spec.res }}'{% if spec.auth %},
        token{% endif %}
      )
    }
