  export function {{ spec.route }} (
    bridgeClient: BridgeClient.T,
    request: {{ spec.req }}{% if spec.auth %},
    token: Text<'AuthToken'>{% endif %}
  ): {% if spec.streaming %}Stream{% else %}Promise{% endif %}<{{ spec.res }}> {
    return {% if spec.streaming %}streaming{% else %}direct{% endif %}<{{ spec.req }}, {{ spec.res }}>(
      bridgeClient,
      '{{ spec.route }}',
      request,
      '{{ spec.res }}'{% if spec.auth %},
      token{% endif %}
    )
  }
